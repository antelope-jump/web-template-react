# Development defaults (picked up automatically by `vite` in development mode)
VITE_API_BASE_URL=/api

# Local backend target when proxy is enabled
VITE_PROXY_TARGET=http://127.0.0.1:8080

# false: use proxy to VITE_PROXY_TARGET
# true: use built-in mock API middleware
VITE_ENABLE_MOCK=false
